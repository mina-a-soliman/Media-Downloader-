name: Build Android APK

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 9.7.1

      - name: Ensure Gradle Wrapper is Ready
        run: |
          if [ ! -f "gradlew" ] || [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "Generating Gradle Wrapper..."
            gradle wrapper --gradle-version 9.7.1 --distribution-type bin
          fi
          chmod +x gradlew

      - name: Build Signed Release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Prepare APK for Artifact Upload
        run: |
          mkdir -p output_apk
          find app/build/outputs/apk/release/ -name "*.apk" -exec ls -lh {} +
          
          # Copy arm64-v8a (Lightweight APK for 99% of modern phones)
          if [ -f "app/build/outputs/apk/release/app-arm64-v8a-release.apk" ]; then
            cp app/build/outputs/apk/release/app-arm64-v8a-release.apk output_apk/MediaDownloader-arm64-v8a.apk
          fi

          # Copy armeabi-v7a (Lightweight APK for older 32-bit phones)
          if [ -f "app/build/outputs/apk/release/app-armeabi-v7a-release.apk" ]; then
            cp app/build/outputs/apk/release/app-armeabi-v7a-release.apk output_apk/MediaDownloader-armeabi-v7a.apk
          fi

          # Copy universal APK if generated
          if [ -f "app/build/outputs/apk/release/app-universal-release.apk" ]; then
            cp app/build/outputs/apk/release/app-universal-release.apk output_apk/MediaDownloader-universal.apk
          elif [ -f "app/build/outputs/apk/release/app-release.apk" ]; then
            cp app/build/outputs/apk/release/app-release.apk output_apk/MediaDownloader-universal.apk
          fi

          echo "=== Prepared APKs ==="
          ls -lh output_apk/

      - name: Upload Recommended Lightweight APK (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: MediaDownloader-Lite-arm64-v8a
          path: output_apk/MediaDownloader-arm64-v8a.apk
          retention-days: 30

      - name: Upload All APK Variants
        uses: actions/upload-artifact@v4
        with:
          name: MediaDownloader-All-Variants
          path: output_apk/*.apk
          retention-days: 30

      - name: Create GitHub Release (On Version Tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: output_apk/*.apk
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
